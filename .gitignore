# OS
.DS_Store

# Dependencies
node_modules

# Build outputs
/build
build/
dist/
.svelte-kit/
/.svelte-kit
/package

# Turbo
.turbo/

# Rust/Cargo
target/
Cargo.lock

# Environment
.env
.env.*
!.env.example

# IDE
.idea/
*.swp
*.swo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Logs
*.log
data/

# Database backups (contain sensitive data)
backups/
*.sql.gz
do-not-commit/

# Runtime-generated Traefik preview configs
config/traefik/dynamic/preview-*.yml

# Reference documentation (large, imported from external source)
docs/reference/agentic-space/

# SDD scratch (any package)
**/.superpowers/

# Playwright MCP scratch artifacts
.playwright-mcp/

# brainstorming visual companion (mockups)
.superpowers/

# Astro build cache. Kept at the ROOT and not only in docs-site/, because a
# .gitignore that lives on one branch disappears when you check out another —
# which is exactly how these files reached main in 20dc588e.
.astro/
